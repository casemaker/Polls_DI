%h1=@poll.title

%p
  = link_to 'Edit', edit_poll_path(@poll), class: "btn btn-default btn-xs"
  = link_to 'Add question', new_poll_question_path(@poll), class: "btn btn-default btn-xs"
  = link_to 'Answer', new_poll_reply_path(@poll), class: "btn btn-default btn-xs"
  \|
  = link_to 'Back', polls_path, class: "btn btn-default btn-xs"

%h2 Questions

%ul
  - @poll.questions.each do |question|  #go through each one of the questions
    %li
      =question.title #print title on screen
 
      %ul
        -question.possible_answers.each do |possible_answer|
          %li=possible_answer.title
